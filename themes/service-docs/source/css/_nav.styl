nav {
  h2 {
    color: #aaa;
    font-size: .9em;
    font-weight: normal;
  }

  .service-list {
    list-style: none;
    margin: 0;
    padding: 0;

    a {
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
      display: block;
      margin: 6px 0;
      color: #000;
      text-decoration: none;
    }
  }
}
